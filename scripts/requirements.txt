# Python dependencies for the data pipeline scripts
# Install with: pip install -r requirements.txt
#
# Note: GDAL must be installed separately via conda or OSGeo4W.
# On Windows: conda install -c conda-forge gdal
# gdal2tiles.py (used in 06_sentinel_heat.py) comes with GDAL.

geopandas>=0.14.0
h3>=3.7.0
shapely>=2.0.0
fiona>=1.9.0
osmnx>=1.9.0
rasterio>=1.3.0
pyproj>=3.6.0
requests>=2.31.0
numpy>=1.26.0
pandas>=2.0.0
